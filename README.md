# bombay

**Bombay** — a Zenoh-native fork of the [kameo](https://github.com/tqwewe/kameo) actor framework: event-sourced, identity-bearing, dataspace-native actors. It replaces kameo's libp2p `remote` layer with a thin layer over a Zenoh `Session`, and adds an adapter that maps [nexus](https://github.com/devrandom-labs/nexus) event-sourced aggregates onto actors (actor = single-writer consistency boundary).

## Bombay + Zenoh + nexus (the co-design)

Bombay sits between two systems and is designed *with* both:

- **Zenoh** owns **transport / addressing / discovery** — actors are addressable in the dataspace by key-expr; `ask` = query/reply, `tell` = put, death-watch = a liveliness token per actor.
- **nexus** owns **consistency / persistence / ordering / replay** — the event log, single-writer-per-aggregate via optimistic concurrency (`Version`), at-least-once + idempotent/exactly-once replay, the forever-driver subscription cursor.
- **Bombay** is the **adapter** that maps nexus aggregates onto Zenoh-addressed actors and builds the runtime nexus ships only primitives for (the loop, dispatch, cursor, lifecycle, supervision, command/projection/saga runners).

Because we control both nexus and bombay, a Zenoh limitation is rarely a Bombay limitation: **for any Zenoh gap, ask whether nexus already covers it, and if not, patch whichever layer gives the best result.** (E.g. Zenoh's per-message "reliability" is only a link-selection marker — real delivery comes from nexus's event log + replay; Zenoh has no in-SDK storage — nexus *is* the store.)

## Status

**M0 → M1 (the fork has landed).** The M0 de-risking is done:
- **#62 walking-skeleton spike — GO.** `ask`/`tell`/liveliness/health validated over Zenoh 1.9.0; sub-ms messaging; crash-detection 2 ms vs partition-detection ~10 s (lease); single-writer must come from nexus, not Zenoh addressing.
- **#64 feature matrix** — Zenoh core vs zenoh-ext vs zenoh-pico mapped, with stability gates and per-card caveats.
- **#63 fork strategy — DECIDED.** Hard-fork (no upstream rebase — the Zenoh rewrite + M7 de-handroll make rebaseability worthless), dual `MIT OR Apache-2.0` (a clean match to kameo). kameo is vendored verbatim from upstream `main` @ `821e247` (latest, identical in code to the v0.21.0 release); see [`NOTICE`](NOTICE) + [`docs/superpowers/specs`](docs/superpowers/specs).

The kameo source (`src/ actors/ console/ macros/` + examples/benches/tests) now sits in-tree, wrapped in the Nix harness (#60). Next: M1 — replace `src/remote/` (libp2p) with a thin Zenoh `Session` layer.

## Reference docs

Distilled, AI-referenceable knowledge lives under [`docs/`](docs/). **Read the relevant doc before working a card:**

- [`docs/zenoh/capabilities.md`](docs/zenoh/capabilities.md) — Zenoh capability matrix (core/ext/pico), stability gates, and the per-card "gap → nexus/bombay coverage" flags. Read before any card touching Zenoh.
- [`docs/testing/`](docs/testing/) — the **card #74 test-coverage spec** for the surviving kameo core (**457 example scenarios + 112 `@property`/`@model` laws** across 42 files) captured as executable `.feature` files. **Card #76 wired the console slice** (PR #81): all 148 console scenarios (tui 93+13, poller 17+4, server_wire 17+4) are green under `nix flake check`, plus an in-process `ratatui::TestBackend` render suite (`console/tests/tui_render.rs`) that drives the real `tui::App` by synthetic keystrokes and lifts its line coverage from 14% to 73%. The reusable World + step-definition pattern (gated `testing` surface, standard-libtest cucumber runner, proptest laws, `max_concurrent_scenarios(1)` for shared global state) is documented in [`docs/testing/README.md`](docs/testing/README.md) for the core/actors cards to follow. The suite is deterministic under CI scheduling/clock variance — ordering invariants assert monotonic `uptime` (an `Instant`), never the best-effort `captured_at` (`SystemTime::now()`, which a CI clock step can regress); and actor-stop assertions use condition-based waiting on the observable registry state, since `wait_for_shutdown()` (mailbox closed) returns before the console monitor records `Stopped`. Catalog: [`invariants.md`](docs/testing/invariants.md), [`properties.md`](docs/testing/properties.md), [`coverage-audit.md`](docs/testing/coverage-audit.md). Scenarios under [`tests/features/`](tests/features/) are tagged `@sequence`/`@lifecycle`/`@boundary`/`@linearizability`; `@bug:<file:line>` probes stay RED (`pubsub.rs:125`, `message_queue.rs:591/707`, `id.rs:140-143`/`:218-221`). **Card #77 wires the core slice** (309 scenarios across 24 files / 12 modules) onto the same harness — design in [`docs/superpowers/specs/2026-06-26-core-cucumber-wiring-design.md`](docs/superpowers/specs/2026-06-26-core-cucumber-wiring-design.md), step-by-step plan in [`docs/superpowers/plans/2026-06-26-core-cucumber-wiring.md`](docs/superpowers/plans/2026-06-26-core-cucumber-wiring.md). The `id.rs` `from_bytes` `@bug` probes are mechanized as `#[should_panic]` demonstrators (green while the defect lives, flip red on fix), and the matching too-short property law in `actor_id.properties.feature` is `@bug`-tagged to keep the gate green.

**Core wiring progress (card #77):** the `actor_id` slice is wired as the reusable example every later module copies — `tests/features/core/actor_id.feature` (17 scenarios) + `actor_id.properties.feature` (5 `@property`/`@model` laws, each driven by a real `proptest!` over boundary-biased `u64` generators `{0, 1, MAX-1, MAX}` plus a deterministic concurrent-contiguity model law) run green via `core_actor_id_bdd` / `core_actor_id_props_bdd`, sharing one `ActorIdWorld` in `tests/core_steps/actor_id.rs`. The three `from_bytes` short-slice `@bug:id.rs:140-143`/`:218-221` scenarios are excluded from the green run and pinned instead by `#[should_panic]` probes (including the real serde `visit_bytes` path) that flip RED when the bounds-check fix lands. The `error` slice is wired next on the same harness — `tests/features/core/error.feature` (20 scenario blocks → 48 Outline rows) + `error.properties.feature` (4 `@property` SendError-algebra laws) run green via `core_error_bdd` / `core_error_props_bdd`, sharing one `ErrorWorld` in `tests/core_steps/error.rs` over `SendError<TestMsg, TestErr>`; it covers `map_msg`/`map_err`/`boxed`/`msg`/`err`/`flatten`, `BoxSendError` downcast round-trip + wrong-type recovery, `unwrap_msg`/`unwrap_err` panic messages, `ActorStopReason::is_normal`, `PanicReason` lifecycle/message classifiers, `PanicError` `with_str`/`with_downcast_ref`/poisoned-mutex access, the lossy-and-non-idempotent `PanicError` serde round-trip (reason prefix compounds; concrete payload type erased), and the local `RegistryError` `BadActorType` vs `NameAlreadyRegistered` domains. No `@bug` scenarios and no `src/` change were needed. The `message` slice is the first to wire REAL SPAWNED ACTORS — `tests/features/core/message.feature` (14 green scenarios) + `message.properties.feature` (5 `@property`/`@model` laws) run via `core_message_bdd` / `core_message_props_bdd`, sharing one `MessageWorld` in `tests/core_steps/message.rs`; it drives the `Message` handler, the `Context` (`actor_ref`/`reply`/`stop`), `reply_sender`/`reply`/`spawn`, ask-vs-tell error routing (an `Err` reply on a tell is classified `PanicReason::OnMessage` and fires `on_panic`; a detached `ctx.spawn` tell-error drives the process-global error hook with `OnMessage` and skips `on_panic`), the `StreamMessage` `Started→Next→Finished` lifecycle via `attach_stream`, and single-writer serialization under real `tokio::spawn`+`Barrier` overlap (50 concurrent asks yield exactly `1..=50`; an ask/tell fold applies each command once with no cross-talk). The laws sweep boundary values `{0, 1, MAX-1, MAX}` over forward round-trips and `HandlerError` identity, and a deterministic `(P, count)` model loop checks the concurrent fold. THREE forward-FAILURE-path scenarios are tagged `@bug:<file:line>` (so the standard `bug*`-tag filter drops them from the green run, leaving 14) and pinned by three red-on-fix probes instead of asserted: `ctx.forward` to a dead target (`@bug:error.rs:293`) and `ctx.try_forward` to a full target (`@bug:error.rs:305`) PANIC the router because `downcast_message::<(M, ReplySender)>().unwrap()` is `None` on the failure path (the caller would otherwise see `ActorStopped`, not the graceful `ActorNotRunning`/`MailboxFull`), and `ctx.blocking_forward` from any async handler (`@bug:ask.rs:461`) panics on tokio `blocking_send` ("Cannot block the current thread from within a runtime"). Each panic is caught actor-internally and surfaced to the router's own `on_panic` hook (the process-global `set_actor_error_hook` is verified NOT to fire for a `HandlerPanic` — it only fires for an `on_stop` panic), so each probe records the `PanicError` message via a custom `on_panic` and asserts the matching panic fired; a graceful fix means no panic → `on_panic` never fires → the probe flips RED (red-on-fix verified empirically by guarding `blocking_send`). Because those three `@bug` scenarios are filtered out and pinned by the probes, their green-path step definitions were dead code and have been removed (along with the `classify_forward_err`/`setup_full_target`/`release_held_target` helpers and the `Hold`/`TryForwardEcho`/`BlockingForwardEcho` test actors only they used). No `src/` change was needed. The `reply` slice wires the `Reply` conversion machinery — `tests/features/core/reply.feature` (22 scenarios, expanding to 35 with the infallible-family Scenario Outline's 13 Examples rows) + `reply.properties.feature` (5 `@property`/`@model` laws) run green via `core_reply_bdd` / `core_reply_props_bdd`, sharing one `ReplyWorld` in `tests/core_steps/reply.rs`; it covers `Result<T,E>` and `impl_infallible_reply!` identity (`to_result`/`into_value`/`into_any_err`, `Error = Infallible`, sampled across unit/scalar/collection/`Arc`/`NonZero*`/atomic/tuple-arity-{1,2,26} families), the single-use `ReplySender` (`send(self, …)` consumes by value — enforced by the type system, exercised through a real delegating handler so the wire `Ok(Box)` / `BoxSendError::HandlerError` mapping is observed), the `DelegatedReply` marker (`into_any_err` is `None`; `to_result`/`into_value` are `unimplemented!` markers — caught panicking on a `Copy` inside a real handler), and `ForwardedReply` (`from_ok`/`from_err`/`from_result` Direct arms, the `Forwarded(Ok(()))`/`Forwarded(Err(send_error))` arms, `downcast_ok`, and the `downcast_err` inner-`HandlerError`-first vs message-less-outer-`SendError` fallback — plus the message-bearing-outer `map_msg` `unreachable!` guard at reply.rs:559 pinned as a documented panic). The laws sweep `{0, 1, MAX-1, MAX}` over Result/forward round-trips and a `N ∈ {2, 8, 64}` model loop checks concurrent forwarded asks recover their own value via `downcast_ok` with no cross-talk under real `tokio::spawn`+`Barrier` overlap. The bare `Forwarded` arm states are reachable only via the `pub(crate)` `ForwardedReply::new` (consumed by the dispatcher), so a minimal `#[cfg(any(test, feature = "testing"))] pub mod testing { pub fn forwarded(..) }` constructor was added to `src/reply.rs` — the only `src/` change in this slice. No `@bug` scenarios. The `actor_ref` slice wires the concurrency-heavy `ActorRef` surface — `tests/features/core/actor_ref.feature` (22 scenarios) + `actor_ref.properties.feature` (5 `@property`/`@model` laws) run green via `core_actor_ref_bdd` / `core_actor_ref_props_bdd`, sharing one `ActorRefWorld` in `tests/core_steps/actor_ref.rs` over REAL SPAWNED ACTORS; it covers ask/tell messaging, the alive/dead state machine (`is_alive` true-then-false; `ActorRef::is_alive` = `!mailbox closed` vs `WeakActorRef::is_alive` = `!shutdown_result initialized`, pinned as `@review-semantics` predicate identities converging post-shutdown), `downgrade`/`upgrade` (Some while a strong ref lives, None once all strong refs drop), strong/weak reference counting, `is_current` (false outside / true inside a handler), id-based identity (eq/hash/ord on specific values + `proptest!` over boundary-biased ids `{0, 1, MAX-1, MAX}`), startup/shutdown waiter ordering and fan-out (10/W concurrent waiters all resolve only after the single completion, verified never-resolves-early), `Recipient`/`ReplyRecipient` type-erasure preserving id + ask/tell + `erase_reply`, and self link/unlink no-ops (observed via the `ActorRef` Debug link-set). All alive/dead and refcount-settle assertions use condition-based polling (never `wait_for_shutdown` as the barrier); the eight `@linearizability` races use real `tokio::spawn`+`Barrier` overlap against an INDEPENDENT integer oracle (100 concurrent tells land exactly `0..100`; 50 concurrent asks each get their own reply; the `@model` strong/weak laws assert an Arc-style DELTA over a measured baseline). The `strong_count and weak_count track live handles` scenario asserts the strong side as a meaningful absolute (`1 → 2` after one clone) and the weak side as a `+1` delta over the at-rest baseline — because kameo's spawn machinery retains internal `WeakSender`s, a freshly spawned default-mailbox actor reports a non-zero `weak_count` at rest, so the absolute weak value is not a stable oracle and only the single-downgrade increment is asserted (`weak_count()` faithfully reports tokio's count). No `@bug` scenarios and no `src/` change were needed. The `actor_lifecycle` slice wires the `Actor` trait's lifecycle hooks and `Spawn` variants over REAL SPAWNED ACTORS — `tests/features/core/actor_lifecycle.feature` (23 scenarios) + `actor_lifecycle.properties.feature` (6 `@property`/`@model` laws) run green via `core_actor_lifecycle_bdd` / `core_actor_lifecycle_props_bdd`, sharing one `LifecycleWorld` in `tests/core_steps/actor_lifecycle.rs`; it covers the startup-buffer replay order (pre-start external messages drained front-to-back; internal self-tells bypass the buffer), the default and overridden `on_panic` `ControlFlow` (`Break(Panicked)`/`Continue`/`Break(reason)`), `on_stop` on kill vs graceful stop, default `on_link_died` (Break for Killed/Panicked/LinkDied, Continue for Normal/SupervisorRestart), the `spawn`/`spawn_with_mailbox`/`spawn_in_thread`/`prepare`/`spawn_link` variants, startup-failure modes, and the RAII last-strong-drop → `Break(Normal)` stop, with all alive/stop observation via condition-based `settle()` polling and the `@linearizability` races (RAII drop, 100 concurrent spawns with pairwise-distinct ids) using real `tokio::spawn`+`Barrier` overlap. Three authoring fixes were made against the TRUE SUT behavior: (1) the `spawn_in_thread` `blocking_send` step now runs on a real blocking thread via `tokio::task::spawn_blocking` (calling `blocking_send` directly on an async runtime worker panics "Cannot block the current thread from within a runtime"); (2) the on_start-PANICS scenario's `Then` was corrected to "stops with reason Panicked **without calling on_stop**" — the `run_actor_lifecycle` Err arm (`spawn.rs:287-319`) never builds the actor and never calls `on_stop` (it needs `&mut self`, which never existed), so the prior log-based on_stop assertion was false; and (3) the startup-buffer-replay property law now spawns its `Recorder` on an UNBOUNDED mailbox so `n=256` pre-start messages can be buffered while `on_start` is blocked (a default bounded(64) `tell().await` deadlocks once full, the consumer being parked in on_start). No `@bug` scenarios and no `src/` change were needed. The `request_ask` slice wires the `AskRequest` builder (mailbox/reply timeouts, `send`/`try_send`/`blocking_send`/`enqueue`/`forward`/`try_forward`/`blocking_forward`/`IntoFuture`) over REAL SPAWNED ACTORS — `tests/features/core/request_ask.feature` (21 green scenarios) + `request_ask.properties.feature` (4 `@property`/`@model` laws) run green via `core_request_ask_bdd` / `core_request_ask_props_bdd`, sharing one `AskWorld` in `tests/core_steps/request_ask.rs`; the 13 `@timing` timeout scenarios each drive their actor + ask inside a DEDICATED current-thread `start_paused(true)` runtime on a blocking thread (tokio's paused clock auto-advances over handler sleeps and ask timeouts, resolving `Timeout(Some(msg))`/`Timeout(None)`/`Ok` deterministically with ~zero wall-clock and no flake), `blocking_*` calls run on real blocking threads via `spawn_blocking`, full-mailbox scenarios park a release-gated `Hold` handler and assert the full observable before draining, and the laws sweep boundary-biased `Duration {ZERO, 1ms, t-1, t, t+1, MAX}` over the `d < t` reply-timeout predicate plus an `N ∈ {2,4,64}` × `c ∈ {1,4,64}` per-caller reply-isolation model. TWO forward-FAILURE scenarios are tagged `@bug:error.rs:293` (`forward` to a stopped actor) and `@bug:error.rs:305` (`try_forward` to a full mailbox) — the same `downcast_message::<(M, ReplySender)>().unwrap()` `None` defect as the `message` slice, but here the panic propagates to the CALLER's thread (the call is not inside a handler), so the standard `bug*`-tag filter drops them and they are pinned by the `bug_forward_stopped_panics` / `bug_try_forward_full_panics` red-on-fix probes in the runner (each asserts the call PANICS; a graceful `ActorNotRunning`/`MailboxFull` fix flips the probe RED). The per-caller-timeout `@model` law gives each caller its OWN actor (delay exactly `d`, no FIFO queueing) so per-caller timeout independence actually holds — a single shared single-writer actor would time out callers from queue position, not their own `d`. The only `src/` change is a `#[cfg(any(test, feature = "testing"))] testing::reply_channel()` constructor added to `src/reply.rs` (builds a `ReplySender<R>` over a fresh oneshot + its readable receiver — `ReplySender::new` is `pub(crate)`, so an out-of-crate test cannot otherwise drive `forward`/`try_forward`/`blocking_forward` and observe the delivered reply). The `request_tell` slice wires the fire-and-forget `TellRequest` builder (`mailbox_timeout`, `send`/`try_send`/`blocking_send`/`send_after`/`IntoFuture`) over REAL SPAWNED ACTORS — `tests/features/core/request_tell.feature` (16 scenarios) + `request_tell.properties.feature` (4 `@phase2` `@property`/`@model` laws) run green via `core_request_tell_bdd` / `core_request_tell_props_bdd`, sharing one `TellWorld` in `tests/core_steps/request_tell.rs`; the `@timing` scenarios drive their actor + tell inside a DEDICATED `start_paused(true)` current-thread runtime on a blocking thread (so `mailbox_timeout` expiry → `Timeout(Some(msg))` and `send_after(d)` delivery resolve deterministically — the abort-before-delay case aborts the `send_after` JoinHandle SYNCHRONOUSLY before the clock advances, proving no delivery + `JoinError::is_cancelled`), `blocking_send` runs on a real blocking thread via `spawn_blocking`, full/busy-mailbox scenarios park a release-gated `Hold` handler and assert the full observable before draining, and the `@linearizability`/`@model` laws sweep capacity `c ∈ {1,2,8,64,1024}` × caller count under real `tokio::spawn`+`Barrier` overlap (each try_send classified by its OWN return value: the recorded set equals exactly the `Ok` set, no `MailboxFull` integer recorded; bounded `send` backpressures so every distinct integer is recorded exactly once). Because a `tell` failure boxes a BARE message, its `From<SendError<Signal>>` conversion downcasts `<M>` (not a tuple) and returns a graceful `ActorNotRunning(M)`/`MailboxFull(M)` — so unlike `ask`'s forward variants there are NO `@bug` scenarios and no caller panic. The `@review-semantics` self-tell scenario captures the debug-build bounded-self-`tell` deadlock warning (`warn_deadlock`, on the `send`/`blocking_send` path only — NOT `try_send`) via a per-thread `tracing-subscriber` layer, asserting the warning fires AND the send still returns `Ok(())` with spare capacity. No `src/` change was needed. The `mailbox` slice wires the bounded/unbounded `Mailbox` mpsc signal channel and its restart `front` push-back buffer DIRECTLY (channel mechanics, not the actor runtime above them) — `tests/features/core/mailbox.feature` (35 scenarios) + `mailbox.properties.feature` (7 `@property`/`@model` laws) run green via `core_mailbox_bdd` / `core_mailbox_props_bdd`, sharing one `MailboxWorld` in `tests/core_steps/mailbox.rs` over a never-spawned `Probe` actor; it covers FIFO delivery, the `push_front` drain-before-channel protocol across `recv`/`blocking_recv`/`poll_recv`/`recv_many`/`blocking_recv_many`/`poll_recv_many` (front drained whole, never mixed with the channel in one call; `push_front` twice prepends the later batch), `len`/`is_empty` counting front+channel, the close/drop/weak-upgrade lifecycle (buffered-drain-then-`None`, strong-count → `is_closed`, weak `upgrade` None after last strong drop), the capacity boundaries (`try_send` → `Full(S)` at capacity, unbounded never-`Full`, `bounded(0)` panics per tokio contract, `capacity`/`max_capacity` `Some`/`None`, `same_channel` true-for-clones / false-cross-kind, `signal_startup_finished` → `MailboxFull` vs `ActorNotRunning`), and the `@linearizability` races under real `tokio::spawn`+`Barrier` overlap (a parked bounded `send` unblocks exactly when `recv` frees a slot; 10×100 concurrent sends drain to exactly 1000 with no loss/dup; per-sender FIFO subsequences stay strictly increasing). The `blocking_send`-parks scenario runs the parked send on a real std thread and frees the slot from the test thread; the `send_timeout` boundary runs in a DEDICATED `start_paused(true)` current-thread runtime so the timeout fires deterministically with ~zero wall-clock; full-channel boundaries fill via non-blocking `try_send` and never `await` a send into a permanently-full channel. The laws sweep boundary-biased caps `{0,1,2,7,64,1024}` × counts (incl. `0,1,c-1,c,c+1,4c`) over FIFO, `try_send`-Full-iff-at-cap, unbounded-never-Full, F-before-C push-front, and post-close-send-returns-the-signal, plus a deterministic per-sender-FIFO refinement (`P ∈ [2,8]`, `k ∈ [1,50]`, exactly-once multiset) and a strong-count→`is_closed` op-sequence model. A minimal `#[cfg(any(test, feature = "testing"))] pub mod testing` was added to `src/mailbox.rs` exposing three items the channel-mechanics scenarios need but the public API cannot reach: `tagged_signal(tag)` / `signal_tag(&signal)` (a production `Signal::Message` is unconstructable out-of-crate — it boxes a real message + `ActorRef` + reply channel — so the tests use a cheap channel-shaped `Signal::LinkDied` whose tag rides in its `ActorId`, read back for ordering/identity assertions) and `push_front` (re-exports the `pub(crate)` `MailboxReceiver::push_front` so the restart push-back protocol can be driven directly). One feature-authoring fix: the `@linearizability` backpressure `Then` was a physically wrapped two-line Gherkin step that the strict `gherkin` parser rejected (the whole feature file failed to parse) — it was joined onto one physical line, preserving the assertion text verbatim (a mechanical unparseable-spec fix, not a SUT change). The `registry` slice wires the local `ActorRegistry` (the value behind the process-global `ACTOR_REGISTRY` Mutex) over REAL SPAWNED ACTORS — `tests/features/core/registry.feature` (20 scenarios) + `registry.properties.feature` (3 `@phase2` `@property`/`@model` laws) run green via `core_registry_bdd` / `core_registry_props_bdd`, sharing one `RegistryWorld` in `tests/core_steps/registry.rs`; it covers `insert` (no-overwrite, dedup-by-name, `bool` winner signal), `get::<A>` (Ok(None) absent / Ok(Some) present+downcast-ok / `Err(BadActorType)` wrong type), `remove`/`remove_by_id` (the `extract_if().next()` first-match-only semantics over one id under two names, asserting only the conserved facts len 2→1→0), `contains_name`/`len`/`is_empty`/`names`/`clear`, empty-string and 100 000-char name keys, and the `@lifecycle` stale-but-present dead-ref behavior (the registry holds a clone and never observes liveness, so a stopped actor stays registered until removed and a stale `get` ref `tell`s back `SendError::ActorNotRunning`). The `@linearizability`/`@model` laws drive same-name election (exactly one of K∈{2,16} concurrent inserts wins, the stored ref is the winner's), distinct-name visibility (32 concurrent inserts all land), and get-during-remove atomicity (only the before/after state, never `BadActorType`/torn) under real `tokio::spawn`+`Barrier` overlap, plus a deterministic insert-NO-overwrite refinement against a `HashMap` reference model. PROCESS-GLOBAL discipline: both runners set `.max_concurrent_scenarios(1)` and each scenario holds its OWN fresh `Arc<Mutex<ActorRegistry>>` (the same shape as the global static, just scenario-scoped) — so absolute-count assertions (`len() == 3/32`, `is_empty()`, `clear` empties) stay faithful with no cross-scenario contamination and no reset hook; the global static itself is exercised end-to-end by the `actor_ref` slice's `register`/`lookup`. No `@bug` scenarios and no `src/` change were needed. The `links` slice wires the per-actor `Links` registry (parent / sibblings / children) and its link/notification machinery — `tests/features/core/links.feature` (17 scenarios) + `links.properties.feature` (4 `@phase2` `@property`/`@model` laws) run green via `core_links_bdd` / `core_links_props_bdd`, sharing one `LinksWorld` in `tests/core_steps/links.rs`; it pins WHO is notified on death and with WHAT payload — a supervised child's `notify_links` hands the parent its `mailbox_rx` + sibling links (so the parent can restart it) while an unsupervised actor's `notify_sibblings` fan-out drains the sibling map and delivers each peer exactly one `LinkDied` with NO `mailbox_rx` (no restart), the death notification is once-only (the `mem::take`/`.drain()` empties the map so a re-driven pass delivers nothing), and the three-step supervisor shutdown runs in order set-flag → send-shutdown → wait-closed (each child's `parent_shutdown` reads true, each `shutdown` closure fires exactly once, `wait_children_closed` stays pending while any child mailbox is open and resolves once all close — zero children resolves immediately). The `parent_shutdown` `AtomicBool` Release/Acquire ordering is pinned both as the drop-branch boundary (a child exiting after the flag is set notifies with NO `mailbox_rx`/siblings and its dropped `mailbox_rx` closes the channel) and as a `@linearizability`/`@model` law: racing the Release store against the child's load-then-notify under real `tokio::spawn`+`Barrier` overlap yields only the two legal shapes (`queue mailbox_rx while flag==false` ∪ `drop while flag==true`), never the forbidden queue-after-observing-true that would deadlock the supervisor; a store-before-load arm asserts the drop branch directly, and K∈[2,16] simultaneous child deaths each notify the shared parent exactly once with their own `mailbox_rx`. The `@property` laws sweep boundary-biased N∈{0,1,2,16,256} siblings × stop-reason∈{Killed,Panicked,LinkDied,Normal} (per-sibling histogram all 1s, all 0s when N==0, never a `mailbox_rx`) and K∈{0,1,2,16} children (one shutdown per child, wait closes exactly those mailboxes). Most scenarios drive the raw `Links` data structure directly via a minimal `#[cfg(any(test, feature = "testing"))] pub mod testing` added to `src/links.rs` (the module is flipped `pub` only under `testing`, `pub(crate)` in production): it exposes `empty_links`/`set_parent`/`add_sibbling`/`add_child`/`child_spec`/`parent_shutdown_flag`/`notify_links` to construct and inspect a `Links` whose notified `Link::Local` sits over a caller-owned `MailboxSender`, plus `recv_link_died` returning a `LinkDiedParts { id, has_mailbox_rx, has_sibblings }` read back from the delivered `Signal::LinkDied` — none of this is reachable out-of-crate otherwise (a `Links` is built only by the spawn machinery and a notified link holds an internal sender whose receiver the run-loop owns). The self-link no-op and the restarted-child-does-not-inherit-a-stale-flag scenarios instead use REAL SPAWNED ACTORS (self-link observed via the `ActorRef` Debug link-set; the flag reset / stale-children clear in the spawn factory proven by a real Permanent-policy child surviving repeated panic→restart, which a stuck `parent_shutdown=true` would block). No `@bug` scenarios.

The work is GitHub-project-cards-driven with TDD; see [`CLAUDE.md`](CLAUDE.md) for the working method, milestones, and engineering rules.

## Building

Bombay uses the same Nix-flake setup as the sibling nexus/agency repos (crane + fenix + flake-utils + advisory-db), with **one deliberate deviation: a pinned STABLE toolchain** (`rust-toolchain.toml`, currently 1.96.0) fed to fenix via `fromToolchainFile`, so Nix and plain `rustup` resolve the *same* toolchain and non-Nix users can build with stock stable Rust.

```bash
direnv allow        # or: nix develop
nix flake check     # the single gate: build + clippy + fmt + audit + deny + nextest + doctest + actionlint
```

> **The `clippy` check passes, but the god-level bar is temporarily relaxed.** The vendored kameo code (~19k LOC) is not clean against bombay's full lint config (1200+ findings, mostly pedantic/nursery/restriction). Rather than bury those under scattered `#[allow]`s on code that ships **zero tests upstream** (notably the `actors` crate), the bar in [`clippy.toml`](clippy.toml) + the `[workspace.lints.clippy]` block (adopted from nexus) is **parked at `allow`** so the gate passes over verbatim kameo — kameo's own `#![warn(clippy::all)]` still holds. It will be re-tightened lint-by-lint, with real fixes, as the surviving core gains test coverage (M1/M7). Both files carry restore instructions. The other checks (fmt/audit/deny/nextest/doctest/actionlint) give real signal.

## Continuous integration

Every pull request to `main` (and every push to a non-`main` branch) runs the single gate — `nix flake check -L` — on GitHub Actions ([`.github/workflows/checks.yml`](.github/workflows/checks.yml), mirroring nexus); `-L` streams full build logs so a failed derivation shows the complete output (e.g. which test/scenario failed) instead of nix's truncated "last 25 lines". That runs the **entire workspace test suite (`bombay-nextest`) and the doc-tests (`bombay-doctest`)** on every change, alongside fmt/audit/deny/clippy. The workflow files are themselves linted by `actionlint`, wired in two places: the `bombay-actionlint` flake check, and the `pre-commit` hook (run eagerly, but only when a workflow file is staged). The clippy gate is green (with the bar relaxed as noted above), so CI now passes end-to-end.

## Local setup

Enable the tracked git hooks once per clone:

```bash
git config core.hooksPath .githooks
```

- **`pre-commit`** — blocks any commit that doesn't stage a `README.md` change (the README-with-every-commit discipline), and runs `actionlint` whenever a GitHub Actions workflow is staged.
- **`pre-push`** — runs the full `nix flake check` before a push; **blocks** on failure (so it will block until the clippy gate is green — bypass a push with `git push --no-verify`).
- **`post-merge`** — runs `nix flake check` after a `git pull`/merge (advisory; git ignores its exit code).
