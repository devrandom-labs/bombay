# bombay

**Bombay** — a Zenoh-native fork of the [kameo](https://github.com/tqwewe/kameo) actor framework: event-sourced, identity-bearing, dataspace-native actors. It replaces kameo's libp2p `remote` layer with a thin layer over a Zenoh `Session`, and adds an adapter that maps [nexus](https://github.com/devrandom-labs/nexus) event-sourced aggregates onto actors (actor = single-writer consistency boundary).

## Bombay + Zenoh + nexus (the co-design)

Bombay sits between two systems and is designed *with* both:

- **Zenoh** owns **transport / addressing / discovery** — actors are addressable in the dataspace by key-expr; `ask` = query/reply, `tell` = put, death-watch = a liveliness token per actor.
- **nexus** owns **consistency / persistence / ordering / replay** — the event log, single-writer-per-aggregate via optimistic concurrency (`Version`), at-least-once + idempotent/exactly-once replay, the forever-driver subscription cursor.
- **Bombay** is the **adapter** that maps nexus aggregates onto Zenoh-addressed actors and builds the runtime nexus ships only primitives for (the loop, dispatch, cursor, lifecycle, supervision, command/projection/saga runners).

Because we control both nexus and bombay, a Zenoh limitation is rarely a Bombay limitation: **for any Zenoh gap, ask whether nexus already covers it, and if not, patch whichever layer gives the best result.** (E.g. Zenoh's per-message "reliability" is only a link-selection marker — real delivery comes from nexus's event log + replay; Zenoh has no in-SDK storage — nexus *is* the store.)

## Status

**M0 → M1 (the fork has landed).** The M0 de-risking is done:
- **#62 walking-skeleton spike — GO.** `ask`/`tell`/liveliness/health validated over Zenoh 1.9.0; sub-ms messaging; crash-detection 2 ms vs partition-detection ~10 s (lease); single-writer must come from nexus, not Zenoh addressing.
- **#64 feature matrix** — Zenoh core vs zenoh-ext vs zenoh-pico mapped, with stability gates and per-card caveats.
- **#63 fork strategy — DECIDED.** Hard-fork (no upstream rebase — the Zenoh rewrite + M7 de-handroll make rebaseability worthless), dual `MIT OR Apache-2.0` (a clean match to kameo). kameo is vendored verbatim from upstream `main` @ `821e247` (latest, identical in code to the v0.21.0 release); see [`NOTICE`](NOTICE) + [`docs/superpowers/specs`](docs/superpowers/specs).

The kameo source (`src/ actors/ console/ macros/` + examples/benches/tests) now sits in-tree, wrapped in the Nix harness (#60). Next: M1 — replace `src/remote/` (libp2p) with a thin Zenoh `Session` layer.

## Reference docs

Distilled, AI-referenceable knowledge lives under [`docs/`](docs/). **Read the relevant doc before working a card:**

- [`docs/zenoh/capabilities.md`](docs/zenoh/capabilities.md) — Zenoh capability matrix (core/ext/pico), stability gates, and the per-card "gap → nexus/bombay coverage" flags. Read before any card touching Zenoh.
- [`docs/testing/`](docs/testing/) — the **card #74 test-coverage spec** for the surviving kameo core, captured as executable `.feature` scenarios (**457 example scenarios + 112 `@property`/`@model` laws** across 42 files; Phase 3 wiring underway — **card #76** wires the console scenarios first (cucumber/proptest/rstest in the workspace dev-deps; the console crate's private `tui` helpers reached through a `#[cfg(feature = "testing")]` surface, run by a standard-harness `cucumber` runner at `console/tests/tui_bdd.rs` with `fmt_short`, `fmt_ago`, `fmt_uptime`, `short_type_name`, `spark_height`, `braille`, `fade_toward_bg`, `color_rgb`, `centered_rect`, `backpressure_style`, and `mailbox_bar` wired — 93 scenarios green under nextest after adding `detect_deadlocks` wiring; tui.feature is fully wired with `fail_on_skipped` and no name-prefix filter; a second runner at `console/tests/tui_props_bdd.rs` wires `tui.properties.feature` IN FULL — all 12 `@property` laws as proptest oracles (boundary-biased generators incl. `0`/`1`/`MAX`) PLUS the `@model` `detect_deadlocks` refinement law against an independent successor-chase cycle finder, 13 scenarios green with the tag filter removed and `fail_on_skipped` on; Phase 2 (poller) begins by extracting `poller.rs`'s size-gate and decode into testable `check_frame_len`/`decode_frame` free fns — `poll()` preserved byte-for-byte — exposed via the gated `testing` surface alongside `MAX_FRAME_BYTES`, then a third runner at `console/tests/poller_bdd.rs` wires the poller's request/reply + framing scenarios — round-trip, one-byte-write + length-prefixed read, slot-publish, advancing-seq, the `MAX_FRAME_BYTES` size-gate boundaries, and the zero-length/invalid-MessagePack decode failures — driving the REAL `Poller::poll` through a `#[cfg(feature = "testing")]` `poll_once_over(stream, slot)` hook over loopback TCP, 9 scenarios green under nextest; Task 13 adds a sibling `poll_once_over_with_read_timeout` hook and wires the 3 truncation/EOF `@boundary` scenarios — short payload (`UnexpectedEof`), short 4-byte prefix (`UnexpectedEof`), MAX prefix with stalled payload (socket read-timeout → `WouldBlock`/`TimedOut` in ~200 ms, not 5 s), 12 scenarios green; Task 14 finishes poller.feature by extracting a testable `connect_attempt` (one `connect_loop` iteration) + a `BACKOFF` const + a bounded `poll_loop_until_error` twin of `poll_loop`, then wiring the 5 `@lifecycle` scenarios — connect-timeout records `Disconnected{error,since}`, the fixed 5 s backoff asserted against `BACKOFF` (never slept), Connecting→Connected on a live listener, mid-poll server death → `Disconnected` + loop returns, and reconnect-then-resume publishing a fresh Snapshot — over loopback against the REAL extracted units; the name-prefix filter is removed so the whole 17-scenario file runs under `fail_on_skipped`, all green; Task 15 adds a fourth runner at `console/tests/poller_props_bdd.rs` wiring `poller.properties.feature` IN FULL — the 4 `@property` laws as proptest oracles: msgpack round-trip identity over an arbitrary `Snapshot` (boundary-biased `seq`/actors/all `ActorStatus` variants/capacity `{None,Some(0),Some(MAX)}`, identity asserted on RE-ENCODED bytes since `Snapshot` has no `PartialEq` — held for ALL generated shapes), the size-gate predicate `L <= MAX_FRAME_BYTES` with named edges `{0,1,MAX-1,MAX,MAX+1,u32::MAX}`, oversized-rejected-before-allocation, and decode totality (any byte string → `Ok`/`InvalidData`, never a panic; slot left untouched), 4 scenarios green with `fail_on_skipped` and no filter; Phase 3 (the ROOT crate's in-tree console server) opens with Task 16 — a root `testing` feature (`testing = ["console"]`) re-exporting the now-`pub` snapshot producer and a `#[cfg(any(test, feature = "testing"))]` `reset_for_test()` that clears the global `REGISTRY` map and zeros the process-global `SEQ`/`TOTAL_SPAWNED`/`REAPED_STOPPED` atomics so each cucumber scenario starts from a known state — exposed as `kameo::console::testing`, with the self dev-dep auto-activating `testing` for the root crate's own test builds; Task 17 adds the first root-crate cucumber runner at `tests/console_wire_bdd.rs` (standard libtest, `required-features = ["testing"]`, root-relative feature path) wiring the two single-connection `@sequence` scenarios from `server_wire.feature` — `reset_for_test()` + a spawned live `Echo` actor, then driving the REAL `kameo::console::testing::snapshot()` producer back-to-back (5× and 2×) and asserting DELTAS: strictly-increasing seq, and exactly-`first + 1` for two snapshots with no concurrent producer; 2 scenarios green and picked up by `cargo nextest run -p kameo` via the self dev-dep; Task 18a wires the three snapshot-driven `server_wire.feature` scenarios — `captured_at`/`uptime` non-decreasing across two spaced polls, the grave-window boundary (an actor stopped within a huge `ttl` is still present as `Stopped` carrying its reason while one stopped for strictly longer than `ttl` ZERO is reaped/absent, pinning the strict `> ttl` predicate), and `total_stopped` conservation across the reap boundary (== 3 == REAPED 2 + 1 present, decomposed from observables) — all driving the REAL `snapshot()`; scenarios run serially via `max_concurrent_scenarios(1)` since they reset and assert absolute counts on the process-global statics, 5 scenarios green; Task 18b wires the four `@boundary` + four `@lifecycle` `server_wire.feature` scenarios against the REAL `kameo::console::serve()` over loopback TCP sockets — the request byte's value is ignored (`0xFF` still yields one frame), pipelined request bytes yield one length-prefixed frame each with strictly-increasing seq, surplus bytes after the request byte are each just another trigger (the server never parses a client-supplied length, so no frame-size cap), a client closing mid-stream ends only its `serve_client` task (a fresh client still gets a snapshot), one client's abrupt disconnect leaves another client's stream serving, a silent client that never writes a byte receives nothing (short read-timeout → `WouldBlock`/`TimedOut`, pull-based), and `shutdown()` aborts the accept loop so subsequent `connect`s are refused; the encode-failure teardown (no length prefix, no partial frame → client sees `UnexpectedEof`) is driven by a minimal `#[cfg(feature = "testing")]` one-shot `fail_next_encode()` hook in `server.rs` (a `SeqCst` `AtomicBool` the serve loop's `encode_snapshot` consumes), 13 scenarios green; Task 19 wires the four `@linearizability` `server_wire.feature` scenarios with REAL overlap (8 `tokio::spawn` tasks gated on a shared `Arc<Barrier>` so the contended work genuinely interleaves) — concurrent producers each take several snapshots and no two share a seq (the global atomic `SEQ.fetch_add` hands out a distinct value per snapshot), a poll during a concurrent spawn batch returns a single consistent registry membership (each id at most once, every entry whole with a well-formed status — the monitor set is cloned under one lock), a poll during a concurrent stop storm shows each id at most once with coherent `Stopped` statuses, and an actor stopped immediately before a poll is still present as `Stopped` within the huge grave window; this FINALIZES the runner — the name-prefix filter is removed so the whole `server_wire.feature` runs under `fail_on_skipped` with `max_concurrent_scenarios(1)`, all 17 scenarios green and stable across repeated runs; Task 20 adds a sixth root-crate runner at `tests/console_wire_props_bdd.rs` wiring `server_wire.properties.feature` IN FULL — the three `@property` `@sequence` laws (seq strictly-increasing AND `seq_i == seq_0 + i` over poll counts `{1,2,5,64,256}`; `captured_at`/`uptime` non-decreasing over `{1,2,8,64}`; `total_stopped == ever_stopped` integer model over spawn/stop/reap schedules incl. the named `{0-stops,1-reaped,1-not-yet-reaped,all-reaped,none-reaped}` boundaries plus seeded random schedules up to length 64, also pinning the unchecked `+` at `registry.rs:454` never wraps) and the `@model` `@linearizability` membership law (≥8 barrier-gated concurrent pollers over spawn/stop interleavings: all produced seqs distinct, every id at most once per snapshot, and each membership a SUBSET of ever-spawned ∧ SUPERSET of the already-registered-never-stopped set — a valid linearization point), driving the REAL `kameo::console::testing::{snapshot,reset_for_test}` via a deterministic boundary-LOOP (the async/proptest bridge is unsuitable for the nested-concurrency `@model` law) that `reset_for_test()`s per case, 4 scenarios green and stable across repeated runs); harness design + task-by-task plan in [`docs/superpowers/`](docs/superpowers/)): [`README.md`](docs/testing/README.md) (Gherkin/`cucumber-rs` methodology + gap report), [`invariants.md`](docs/testing/invariants.md) (every behavioural invariant, grounded in `file:line`), [`properties.md`](docs/testing/properties.md) (the Phase-2 `@property`/`@model` law catalog), and [`coverage-audit.md`](docs/testing/coverage-audit.md) (input-class completeness audit + the gaps it closed — see *Addendum 2* for the Phase-2b P2/vague-Then/doc-sync pass). Scenarios live under [`tests/features/`](tests/features/) tagged `@sequence`/`@lifecycle`/`@boundary`/`@linearizability`; known defects are `@bug:<file:line>` probes that must stay RED (`pubsub.rs:125`, `message_queue.rs:591/707`, and the new `ActorId::from_bytes` too-short-decode panic at `id.rs:140-143`/`:218-221`).

The work is GitHub-project-cards-driven with TDD; see [`CLAUDE.md`](CLAUDE.md) for the working method, milestones, and engineering rules.

## Building

Bombay uses the same Nix-flake setup as the sibling nexus/agency repos (crane + fenix + flake-utils + advisory-db), with **one deliberate deviation: a pinned STABLE toolchain** (`rust-toolchain.toml`, currently 1.96.0) fed to fenix via `fromToolchainFile`, so Nix and plain `rustup` resolve the *same* toolchain and non-Nix users can build with stock stable Rust.

```bash
direnv allow        # or: nix develop
nix flake check     # the single gate: build + clippy + fmt + audit + deny + nextest + doctest + actionlint
```

> **The `clippy` check passes, but the god-level bar is temporarily relaxed.** The vendored kameo code (~19k LOC) is not clean against bombay's full lint config (1200+ findings, mostly pedantic/nursery/restriction). Rather than bury those under scattered `#[allow]`s on code that ships **zero tests upstream** (notably the `actors` crate), the bar in [`clippy.toml`](clippy.toml) + the `[workspace.lints.clippy]` block (adopted from nexus) is **parked at `allow`** so the gate passes over verbatim kameo — kameo's own `#![warn(clippy::all)]` still holds. It will be re-tightened lint-by-lint, with real fixes, as the surviving core gains test coverage (M1/M7). Both files carry restore instructions. The other checks (fmt/audit/deny/nextest/doctest/actionlint) give real signal.

## Continuous integration

Every pull request to `main` (and every push to a non-`main` branch) runs the single gate — `nix flake check` — on GitHub Actions ([`.github/workflows/checks.yml`](.github/workflows/checks.yml), mirroring nexus). That runs the **entire workspace test suite (`bombay-nextest`) and the doc-tests (`bombay-doctest`)** on every change, alongside fmt/audit/deny/clippy. The workflow files are themselves linted by `actionlint`, wired in two places: the `bombay-actionlint` flake check, and the `pre-commit` hook (run eagerly, but only when a workflow file is staged). The clippy gate is green (with the bar relaxed as noted above), so CI now passes end-to-end.

## Local setup

Enable the tracked git hooks once per clone:

```bash
git config core.hooksPath .githooks
```

- **`pre-commit`** — blocks any commit that doesn't stage a `README.md` change (the README-with-every-commit discipline), and runs `actionlint` whenever a GitHub Actions workflow is staged.
- **`pre-push`** — runs the full `nix flake check` before a push; **blocks** on failure (so it will block until the clippy gate is green — bypass a push with `git push --no-verify`).
- **`post-merge`** — runs `nix flake check` after a `git pull`/merge (advisory; git ignores its exit code).
