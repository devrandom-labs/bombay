[package]
name = "bombay-machine"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic state machines and ordered execution for Bombay."
keywords = ["concurrency", "reducer", "state-machine"]
categories = ["concurrency"]

[dependencies]
thiserror.workspace = true

[dev-dependencies]
loom = "0.7.2"

[target.'cfg(loom)'.dependencies]
loom = "0.7.2"

[lints]
workspace = true
