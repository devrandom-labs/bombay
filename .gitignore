target/
# cargo-mutants run output (the mutation gate writes here)
mutants.out/
mutants.out.old/
# nix build result symlink
result
.direnv
.DS_Store
.makemd
.obsidian
.space

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# local Claude Code settings (personal)
.claude/settings.local.json

# session scratch (agent tooling, visual-companion, local backups)
.superpowers/
.auto/*.local-preserved
.auto/refs/
.auto/review.md
