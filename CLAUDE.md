# bombay agent entry point

Follow `AGENTS.md` for repository rules. The live architecture documents are:

- `docs/open-design-ledger.md` for backlog state and cross-crate evidence;
- `docs/module-boundaries.md` for source ownership;
- `docs/minimal-lifecycle-ownership.md` for lifecycle ordering;
- `docs/runtime-blocks.md` for crate scope.

Application authors should use `docs/cookbook.md` for current typed effect
composition. That guide is subordinate to the live architecture documents
above.

No other design document is authoritative.
