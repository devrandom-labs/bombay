# Examples

Repository-level Bombay examples belong in this directory.

Every example here uses only Bombay's public API. Start with `basic.rs` for a
complete behavior definition, local spawn, and typed send.
